#!/bin/sh
# Pre-commit hook: run type-check and lint before committing.
# Installed automatically via `bun install` (see "prepare" script in package.json).

echo "Running type-check..."
bun run tsc || exit 1

echo "Running linter..."
bun run lint || exit 1
